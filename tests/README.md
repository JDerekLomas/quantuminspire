# Tests

- Run all tests: `npm test` (Vitest).
- Current coverage: `lib/quantum.ts` (gates/states/Bloch/entropy), `lib/rabi.ts` (Rabi math).

Add new unit tests under `tests/` mirroring module names.***
