import PageClient from './PageClient'

export const metadata = {
  title: 'How might AI accelerate quantum?',
  description: 'AI agents run real quantum experiments on real hardware through natural language. 6 papers replicated, 93% of claims pass. Open research from TU Delft / QuTech.',
}

export default function HomePage() {
  return <PageClient />
}
