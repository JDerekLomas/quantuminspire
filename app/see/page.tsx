import PageClient from './PageClient'

export const metadata = {
  title: 'See a Qubit — Inside Quantum Hardware',
  description: 'Scroll through the physical layers of a quantum computer — from the dilution fridge to the chip, Josephson junction, drive lines, and entangling gates.',
}

export default function SeePage() {
  return <PageClient />
}
