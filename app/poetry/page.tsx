import PageClient from './PageClient'

export const metadata = {
  title: 'Quantum Poetry — haiqu',
  description: 'Poetry is the literary form most naturally suited to quantum computation. Experiments in superposition, entanglement, and measurement on real quantum hardware.',
}

export default function PoetryPage() {
  return <PageClient />
}
