import PageClient from './PageClient'

export const metadata = {
  title: 'Measurement — How Qubits Work',
  description: 'Dispersive readout, IQ plane discrimination, and the quantum-to-classical boundary. How we extract information from superconducting qubits.',
}

export default function MeasurementPhysicsPage() {
  return <PageClient />
}
