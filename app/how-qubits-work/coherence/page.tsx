import PageClient from './PageClient'

export const metadata = {
  title: 'Coherence — How Qubits Work',
  description: 'T₁ relaxation, T₂ dephasing, and why qubits forget. Interactive decay curves and the race against decoherence in superconducting hardware.',
}

export default function CoherencePage() {
  return <PageClient />
}
