import PageClient from './PageClient'

export const metadata = {
  title: 'How Qubits Work — 6-Part Physics Series',
  description: 'From spectroscopy to scaling: how superconducting qubits are found, controlled, coupled, measured, and scaled. Interactive physics deep dives.',
}

export default function HowQubitsWorkPage() {
  return <PageClient />
}
