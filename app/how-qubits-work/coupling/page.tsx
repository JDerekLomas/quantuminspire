import PageClient from './PageClient'

export const metadata = {
  title: 'Coupling — How Qubits Work',
  description: 'How qubits talk to each other. Capacitive coupling, bus resonators, and the physics of two-qubit interactions in superconducting processors.',
}

export default function CouplingPage() {
  return <PageClient />
}
