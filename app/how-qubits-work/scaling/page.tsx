import PageClient from './PageClient'

export const metadata = {
  title: 'Scaling — How Qubits Work',
  description: 'From 1 qubit to 1000. Wiring, crosstalk, frequency collisions, and the engineering challenges of scaling superconducting quantum processors.',
}

export default function ScalingPage() {
  return <PageClient />
}
