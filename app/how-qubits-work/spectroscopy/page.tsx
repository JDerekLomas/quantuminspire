import PageClient from './PageClient'

export const metadata = {
  title: 'Spectroscopy — How Qubits Work',
  description: 'How we find a qubit\'s frequency. Energy levels, resonance peaks, and the physics of microwave-qubit interaction in superconducting processors.',
}

export default function SpectroscopyPage() {
  return <PageClient />
}
