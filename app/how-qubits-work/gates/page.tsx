import PageClient from './PageClient'

export const metadata = {
  title: 'Gates — How Qubits Work',
  description: 'How microwave pulses become quantum logic gates. Single-qubit rotations, CNOT via cross-resonance, and gate calibration on real hardware.',
}

export default function GatesPage() {
  return <PageClient />
}
